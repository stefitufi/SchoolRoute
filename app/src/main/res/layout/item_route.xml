<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="92dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/buses_civ"
        android:layout_width="106dp"
        android:layout_height="106dp"
        app:civ_border_width="0dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"/>
    <TextView
        android:id="@+id/name_ruta_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/description_ruta_tv"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/description_ruta_tv"
        android:alpha="0.7"
        android:text="Name"
        android:textSize="18sp" />
    <TextView
        android:id="@+id/description_ruta_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="17dp"
        android:layout_marginStart="17dp"
        android:layout_toEndOf="@+id/buses_civ"
        android:layout_toRightOf="@+id/buses_civ"
        android:alpha="0.5"
        android:text="Descriotion"
        android:textSize="16sp" />

</RelativeLayout>